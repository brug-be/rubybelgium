<%
imgs = {}
Dir.chdir("#{app.root_path}/source/assets/images/") do
  imgs = Dir["**/*"].inject({}) {|h,f| h.merge! f => image_path(f)}
end
%>

window.image_path = (name) ->
  <%= imgs.to_json %>[name]