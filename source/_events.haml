%section#events
  .container
    .row
      .col-lg-12.text-center
        %h2.section-heading Next Events
    .row
      .col-lg-12
        %ul.timeline
          - data.events.each_with_index do |event, index|
            %li{:class => index.odd? ? 'timeline-inverted' : ''}
              .timeline-image
                - if event.image
                  = link_to_if event.link.present?, image_tag(event.image, class: "img-circle img-responsive", alt: event.date), event.link
              .timeline-panel
                .timeline-heading
                  %h4= event.date
                  %h4.subheading= link_to_if event.link.present?, event.title, event.link
                .timeline-body
                  %p.text-muted= event.description
          %li.timeline-inverted
            .timeline-image
              %h4
                Be Part
                %br
                Of Our
                %br
                Story!
